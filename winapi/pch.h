#pragma once

// Windows Headers
#define WIN32_LEAN_AND_MEAN             // Exclude rarely-used stuff from Windows headers
#include <Windows.h>

// C++ Headers
#include <iostream>
#include <optional>
#include <string>