#pragma once

// Windows Headers
#define WIN32_LEAN_AND_MEAN             // Exclude rarely-used stuff from Windows headers
#include <Windows.h>
#define SECURITY_WIN32
#include <security.h>

// C++ Headers
#include <optional>
#include <string>
#include <functional>